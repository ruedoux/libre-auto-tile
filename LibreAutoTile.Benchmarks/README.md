# LibreAutoTile Benchmarks

Benchmarks built with [BenchmarkDotNet](https://benchmarkdotnet.org/).

## Running

From the repository root:

```sh
./build.sh --run-benchmark
```

Or directly:

```sh
dotnet run -c Release --filter '*'
```

## Results

[View the latest benchmark results](BenchmarkResults.md).
