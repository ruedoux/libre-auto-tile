# LibreAutoTile Core Library Tests

To run all tests

```sh
dotnet run
```

To run single test class

```sh
dotnet run --test-class ClassName
```

To run single method

```sh
dotnet run --test-class ClassName --test-method MethodName
```
